generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Producto {
  id          String  @id @default(uuid())
  nombre      String
  precio      Float
  descripcion String?
  imagenUrl   String?
}
